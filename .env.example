GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Used server-side only, for two admin operations: creating a pre-confirmed
# user at sign-up (lib/supabase/admin.ts, app/api/auth/register) and deleting a
# user on account closure (app/api/account). Never expose this to the client.
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_SITE_URL=http://localhost:3000
